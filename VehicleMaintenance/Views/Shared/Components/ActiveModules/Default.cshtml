@model List<Module>

@if (Model != null && Model.Any())
{
    foreach (var module in Model)
    {
        <li class="nav-item">
            <a class="nav-link" href="/@module.ModuleName/Index">
                <i class="nav-link"></i> @module.ModuleNameTR
            </a>
        </li>
    }
}
